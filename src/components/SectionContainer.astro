---
interface Props {
  class?: string;
  id?: string;
}

const { class: className = "", id } = Astro.props;
---

<div id={id} class:list={["max-w-[1800px] mx-auto px-4 sm:px-8 lg:px-12", className]}>
  <slot />
</div>
